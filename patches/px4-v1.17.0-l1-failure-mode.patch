--- a/msg/versioned/VehicleStatus.msg
+++ b/msg/versioned/VehicleStatus.msg
@@ -49,7 +49,7 @@
 uint8 NAVIGATION_STATE_TERMINATION = 13         # Termination mode
 uint8 NAVIGATION_STATE_OFFBOARD = 14
 uint8 NAVIGATION_STATE_STAB = 15                # Stabilized mode
-uint8 NAVIGATION_STATE_FREE1 = 16
+uint8 NAVIGATION_STATE_L1_FAILURE = 16          # L1 adaptive single-motor failure mode
 uint8 NAVIGATION_STATE_AUTO_TAKEOFF = 17        # Takeoff
 uint8 NAVIGATION_STATE_AUTO_LAND = 18           # Land
 uint8 NAVIGATION_STATE_AUTO_FOLLOW_TARGET = 19  # Auto Follow
--- a/src/modules/commander/module.yaml
+++ b/src/modules/commander/module.yaml
@@ -39,6 +39,7 @@
                 12: Follow Me
                 13: Precision Land
                 16: Altitude Cruise
+                17: L1 Failure
                 100: External Mode 1
                 101: External Mode 2
                 102: External Mode 3
--- a/src/modules/manual_control/ManualControl.cpp
+++ b/src/modules/manual_control/ManualControl.cpp
@@ -591,6 +591,7 @@
 		case 14: return vehicle_status_s::NAVIGATION_STATE_ORBIT;
 		case 15: return vehicle_status_s::NAVIGATION_STATE_AUTO_VTOL_TAKEOFF;
 		case 16: return vehicle_status_s::NAVIGATION_STATE_ALTITUDE_CRUISE;
+		case 17: return vehicle_status_s::NAVIGATION_STATE_L1_FAILURE;
 
 		case 100: return vehicle_status_s::NAVIGATION_STATE_EXTERNAL1;
 		case 101: return vehicle_status_s::NAVIGATION_STATE_EXTERNAL2;
--- a/src/modules/commander/px4_custom_mode.h
+++ b/src/modules/commander/px4_custom_mode.h
@@ -79,7 +79,8 @@
 enum PX4_CUSTOM_SUB_MODE_POSCTL {
 	PX4_CUSTOM_SUB_MODE_POSCTL_POSCTL = 0,
 	PX4_CUSTOM_SUB_MODE_POSCTL_ORBIT,
-	PX4_CUSTOM_SUB_MODE_POSCTL_SLOW
+	PX4_CUSTOM_SUB_MODE_POSCTL_SLOW,
+	PX4_CUSTOM_SUB_MODE_POSCTL_L1_FAILURE
 };
 
 union px4_custom_mode {
@@ -126,6 +127,11 @@
 		custom_mode.sub_mode = PX4_CUSTOM_SUB_MODE_POSCTL_SLOW;
 		break;
 
+	case vehicle_status_s::NAVIGATION_STATE_L1_FAILURE:
+		custom_mode.main_mode = PX4_CUSTOM_MAIN_MODE_POSCTL;
+		custom_mode.sub_mode = PX4_CUSTOM_SUB_MODE_POSCTL_L1_FAILURE;
+		break;
+
 	case vehicle_status_s::NAVIGATION_STATE_AUTO_MISSION:
 		custom_mode.main_mode = PX4_CUSTOM_MAIN_MODE_AUTO;
 		custom_mode.sub_mode = PX4_CUSTOM_SUB_MODE_AUTO_MISSION;
--- a/src/modules/commander/Commander.cpp
+++ b/src/modules/commander/Commander.cpp
@@ -825,6 +825,10 @@
 
 					case PX4_CUSTOM_SUB_MODE_POSCTL_SLOW:
 						desired_nav_state = vehicle_status_s::NAVIGATION_STATE_POSITION_SLOW;
+						break;
+
+					case PX4_CUSTOM_SUB_MODE_POSCTL_L1_FAILURE:
+						desired_nav_state = vehicle_status_s::NAVIGATION_STATE_L1_FAILURE;
 						break;
 					}
 
--- a/src/modules/commander/ModeUtil/control_mode.cpp
+++ b/src/modules/commander/ModeUtil/control_mode.cpp
@@ -84,6 +84,19 @@
 		vehicle_control_mode.flag_control_allocation_enabled = true;
 		break;
 
+	case vehicle_status_s::NAVIGATION_STATE_L1_FAILURE:
+		vehicle_control_mode.flag_control_manual_enabled = true;
+		vehicle_control_mode.flag_control_position_enabled = true;
+		vehicle_control_mode.flag_control_velocity_enabled = true;
+		vehicle_control_mode.flag_control_altitude_enabled = true;
+		vehicle_control_mode.flag_control_climb_rate_enabled = true;
+		vehicle_control_mode.flag_control_attitude_enabled = true;
+		// L1 publishes vehicle torque directly; keep the native rate controller
+		// running but prevent it from publishing a competing torque setpoint.
+		vehicle_control_mode.flag_control_rates_enabled = false;
+		vehicle_control_mode.flag_control_allocation_enabled = true;
+		break;
+
 	case vehicle_status_s::NAVIGATION_STATE_AUTO_RTL:
 	case vehicle_status_s::NAVIGATION_STATE_AUTO_LAND:
 	case vehicle_status_s::NAVIGATION_STATE_AUTO_PRECLAND:
--- a/src/modules/commander/ModeUtil/conversions.hpp
+++ b/src/modules/commander/ModeUtil/conversions.hpp
@@ -54,6 +54,8 @@
 
 	case vehicle_status_s::NAVIGATION_STATE_POSCTL: return navigation_mode_t::posctl;
 
+	case vehicle_status_s::NAVIGATION_STATE_L1_FAILURE: return navigation_mode_t::posctl;
+
 	case vehicle_status_s::NAVIGATION_STATE_AUTO_MISSION: return navigation_mode_t::auto_mission;
 
 	case vehicle_status_s::NAVIGATION_STATE_AUTO_LOITER: return navigation_mode_t::auto_loiter;
--- a/src/modules/commander/ModeUtil/mode_requirements.cpp
+++ b/src/modules/commander/ModeUtil/mode_requirements.cpp
@@ -83,6 +83,15 @@
 	setRequirement(vehicle_status_s::NAVIGATION_STATE_POSCTL, flags.mode_req_local_position_relaxed);
 	setRequirement(vehicle_status_s::NAVIGATION_STATE_POSCTL, flags.mode_req_manual_control);
 
+	// NAVIGATION_STATE_L1_FAILURE
+	setRequirement(vehicle_status_s::NAVIGATION_STATE_L1_FAILURE, flags.mode_req_angular_velocity);
+	setRequirement(vehicle_status_s::NAVIGATION_STATE_L1_FAILURE, flags.mode_req_attitude);
+	setRequirement(vehicle_status_s::NAVIGATION_STATE_L1_FAILURE, flags.mode_req_local_alt);
+	setRequirement(vehicle_status_s::NAVIGATION_STATE_L1_FAILURE, flags.mode_req_local_position_relaxed);
+	setRequirement(vehicle_status_s::NAVIGATION_STATE_L1_FAILURE, flags.mode_req_manual_control);
+	// Enter only after a normal takeoff; never arm with a motor already disabled.
+	setRequirement(vehicle_status_s::NAVIGATION_STATE_L1_FAILURE, flags.mode_req_prevent_arming);
+
 	// NAVIGATION_STATE_POSITION_SLOW
 	setRequirement(vehicle_status_s::NAVIGATION_STATE_POSITION_SLOW, flags.mode_req_angular_velocity);
 	setRequirement(vehicle_status_s::NAVIGATION_STATE_POSITION_SLOW, flags.mode_req_attitude);
--- a/src/modules/flight_mode_manager/FlightModeManager.cpp
+++ b/src/modules/flight_mode_manager/FlightModeManager.cpp
@@ -204,7 +204,9 @@
 	}
 
 	// Manual position control
-	if ((_vehicle_status_sub.get().nav_state == vehicle_status_s::NAVIGATION_STATE_POSCTL) || task_failure) {
+	if ((_vehicle_status_sub.get().nav_state == vehicle_status_s::NAVIGATION_STATE_POSCTL)
+	    || (_vehicle_status_sub.get().nav_state == vehicle_status_s::NAVIGATION_STATE_L1_FAILURE)
+	    || task_failure) {
 		found_some_task = true;
 		FlightTaskError error = FlightTaskError::NoError;
 
--- a/src/lib/modes/ui.hpp
+++ b/src/lib/modes/ui.hpp
@@ -57,6 +57,7 @@
 	       (1u << vehicle_status_s::NAVIGATION_STATE_TERMINATION) |
 	       (1u << vehicle_status_s::NAVIGATION_STATE_OFFBOARD) |
 	       (1u << vehicle_status_s::NAVIGATION_STATE_STAB) |
+	       (1u << vehicle_status_s::NAVIGATION_STATE_L1_FAILURE) |
 	       (1u << vehicle_status_s::NAVIGATION_STATE_AUTO_TAKEOFF) |
 	       (1u << vehicle_status_s::NAVIGATION_STATE_AUTO_LAND) |
 	       (1u << vehicle_status_s::NAVIGATION_STATE_AUTO_FOLLOW_TARGET) |
@@ -84,7 +85,7 @@
 	"Termination",
 	"Offboard",
 	"Stabilized",
-	"16: UNUSED2",
+	"L1 Failure",
 	"Takeoff",
 	"Land",
 	"Follow Target",
--- a/ROMFS/px4fmu_common/init.d/rc.mc_apps
+++ b/ROMFS/px4fmu_common/init.d/rc.mc_apps
@@ -15,6 +15,13 @@
 #
 mc_rate_control start
 
+if param compare L1_FAIL_EN 1
+then
+	param set -s SYS_FAILURE_EN 1
+	param set -s CA_FAILURE_MODE 1
+	l1_adaptive_control start
+fi
+
 #
 # Start Multicopter Attitude Controller.
 #
